import { useState } from 'react';
import { ScrollView, StatusBar,SafeAreaView, StyleSheet, Text, View ,Platform  } from 'react-native';
import { theme } from '../colors';

export default function First() {

      return (
        <View>
            <Text>falsesdfsdf</Text>
        </View>
      );
  }